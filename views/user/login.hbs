{{!-- <div class=" row">
   <div class="col-md-6 m-auto">
      <div class="container mt-5 pl-5">
          <h2 class="text-center">Login</h2>
          <form action="/login" method="POST" id="form">
          {{#if loginErr}}
            <p class="error">Invalid email or password</p>
            {{/if}}
             <div class="form-group  " >
                 <label>Email address</label>
                 <input type="email" class="form-control" name="Email" id="email" placeholder="Enter email"  >
             </div>
             <div id="errorError" class="text-danger" > </div>

            
             <div class="form-group">
                 <label>Password</label>
                 <input type="password" class="form-control" name="Password" id="password" placeholder="Password"  >
             </div >
             <div id="errorPassword" class="text-danger"></div>
              <a href="/signup">Create new account ?</a>
             <button type="submit" name="submit" class="btn btn-primary float-right mt-5">Login</button>
          </form>
      </div>
   </div>
</div> --}}
<section class="vh-100 gradient-customs">
  <div class="container py-5 h-100">
    <div class="row d-flex justify-content-center align-items-center h-100">
      <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="card bg-dark text-white" style="border-radius: 1rem;">
          <div class="card-body p-5 text-center">

            <div class="mb-md-5 mt-md-4 pb-5">
               <form action="/login" method="POST" id="form">
                 {{#if loginErr}}
            <p class="error">{{loginErr}} </p>
            {{/if}}

              <h2 class="fw-bold mb-2 text-uppercase">Login</h2>
              <p class="text-white-50 mb-5">Please enter your login and password!</p>
 <span id="errorName"></span>
              <div class="form-outline form-white mb-4">
                <input type="email" id="email" name="Email" class="form-control form-control-lg" required/>
                <label class="form-label" for="typeEmailX">Email</label>
               
                
              </div>
              

              <div class="form-outline form-white mb-4">
                <input type="password" id="password" name="Password" class="form-control form-control-lg" required/>
                <label class="form-label" for="typePasswordX">Password</label>
              </div>

              <p class="small mb-5 pb-lg-2"><a class="text-white-50" href="/otp-login">otp login</a></p>

              <button class="btn btn-outline-light btn-lg px-5" type="submit">Login</button>
              </form>

              <div class="d-flex justify-content-center text-center mt-4 pt-1">
                <a href="#!" class="text-white"><i class="fab fa-facebook-f fa-lg"></i></a>
                <a href="#!" class="text-white"><i class="fab fa-twitter fa-lg mx-4 px-2"></i></a>
                <a href="#!" class="text-white"><i class="fab fa-google fa-lg"></i></a>
              </div>

            </div>

            <div>
              <p class="mb-0">Don't have an account? <a href="/signup" class="text-white-50 fw-bold">Sign Up</a>
              </p>
            </div>

          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const email = document.getElementById("email");
        const password = document.getElementById("password")
        const form = document.getElementById("form");
        const error1 = document.getElementById("errorName")
        const error2 = document.getElementById("errorPassword")
       

         form.addEventListener('click', (e) => {
            
          
            if (email.value === '' || email.value == null) {
               error1.innerHTML="please enter email address";
            }
            if (password.value === '' || password.value == null) {
               error2.innerHTML="please enter password";
            }
            else if (password.value.length < 5) {
                error2.innerHTML="Password must be at least 5 char"
            }
           
        }) 
email.addEventListener('input',()=>{
  const emailPattern = /[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{1,63}$/;

  if(email.value.match(emailPattern)){
    error1.innerHTML = ""; 
  }else{
    error1.innerHTML="Must be a valid email address"; 
  }
});

</script>

{{!-- <style>
  body {
    background-color: #f1dfd1;
    background-image: linear-gradient(315deg, #f1dfd1 0%, #f6f0ea 74%);

  }
</style> --}}